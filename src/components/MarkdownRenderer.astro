---
import { Markdown } from 'astro/markdown';

const { markdownContent } = Astro.props;
---
<div class="markdown-container">
  <Markdown content={markdownContent} />
</div>

<style>
  .markdown-container {
    overflow-x: auto; /*  处理表格在小屏幕下的水平滚动 */
  }

  .markdown-container table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 1em;
  }

  .markdown-container th,
  .markdown-container td {
    border: 1px solid rgba(255, 255, 255, 0.2); /*  与卡片边框颜色一致 */
    padding: 0.5em;
    text-align: left;
    color: #eee; /*  与标签文字颜色一致 */
  }

  .markdown-container th {
    font-weight: bold;
    background-color: rgba(255, 255, 255, 0.1); /*  表头背景色 */
  }

  .markdown-container td img {
    vertical-align: middle; /*  徽标垂直居中 */
    margin-right: 0.3em;
  }
</style>
